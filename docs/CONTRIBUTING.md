# Contributing

1. **Create a branch** from latest `master` (`git pull --rebase`).
2. **Follow the PR template** and include an Impact Note.
3. **Run checks** locally: type, lint, tests.
4. **Keep diffs small**; one logical change per PR.
5. **Update docs** (README/ADR/Runbooks) when behavior or architecture changes.

